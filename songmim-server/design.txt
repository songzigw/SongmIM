1. 客户端与服务器端的行为定义

/** 连接授权 */
CONN_AUTH(1),
/** 连接关闭 */
CONN_CLOSE(2),
/** 客户端发起心跳 */
HEARTBEAT(3),
/** 发布消息到服务端 */
PUBLISH_MSG(4),
/** 转发消息到客户端 */
BROKER_MSG(5),  

基于TCP/IP协议的API接口
基于HTTP协议的API接口

（为什么用数字表示，而不用单词表示，而且单词表示，
具有很好的可读性；虽然这样，但是随着业务的增加，
数字是不变的，可是它的意思会发生略微的变化，这个时候可以改变为另外的单词）

2. 客户端与服务器通信协议规定

包头--共占20个字节
版本号(version) 占2个字节
头部长度(headerLen) 占2个字节
数据包长度(packetLen) 占4个字节
数据包编号(sequence) 占8个字节
操作行为码(operation) 占4个字节

包体
数据包，包体部分(body)

3. 错误码定义

/** Token无效 */
TOKEN_INVALID,
/** Session失效 */
SESSION_DISABLED,
/** 签名失败 */
SIGN_FAILURE,
/** 消息来源无效 */
MSG_SOURCE_INVALID

4. 消息按照会话类型分类
私聊 群聊 收到新闻 收到评论 收到点赞

5. 消息按照会话内容分类
未读数消息 文本消息 图片消息 正在写状态

6. 后台 HTTP API 接口

7. 基于线程池的消息队列处理机制（目前没有实现）

8. MQ核心组件：MQ服务高度抽象层、消息事件监听模型、消息队列模型、异步回调模型

9. 已读设置???